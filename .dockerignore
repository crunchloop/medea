# Keep the build context to what `deploy/Dockerfile` actually compiles
# (Go module + sources). Everything below is docs, tooling, or local state.
.git
.github
.claude
design
deploy
scripts
*.md
.golangci.yml
buf.yaml
buf.gen.yaml
# Local Talos machine configs occasionally dropped at the repo root.
controlplane.yaml
worker.yaml
