# build output
/bin/
/medea

# runtime state (never commit the live store or its desired-state export)
*.db
medea-desired.json

# design notes (in-flight); also covered by repo-root .gitignore
design/private/

# talosctl test artifacts (`talosctl cluster create` drops these in cwd during
# `make test-qemu`) — throwaway, never commit
/controlplane.yaml
/worker.yaml

# goreleaser build output
/dist/
